<Window x:Class="Tickets.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:i="clr-namespace:Tickets.Infrastructure"
        xmlns:vm="clr-namespace:Tickets.ViewModels"
        xmlns:views="clr-namespace:Tickets.Views"
        Title="Tickets" Height="700" Width="1200" 
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource MaterialDesignWindow}">
    
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    
    <Grid>
        <ContentControl Content="{Binding CurrentView}" Focusable="False">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type vm:LogInViewModel}">
                    <views:LogInView />
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:SignInViewModel}">
                    <views:SignInView />
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:RoutesViewModel}">
                    <views:RoutesView />
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:ProfileViewModel}">
                    <views:ProfileView />
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
    </Grid>
</Window>
